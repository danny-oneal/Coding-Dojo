<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Playground 1</title>
    <style>
        body {
            text-align: center;
            padding: 5rem;
        }

        .box-container {
            display: flex;
            flex-wrap: wrap;
            gap: 2rem;
            width: 100%;
            justify-content: center;
        }

        .box {
            width: 250px;
            height: 250px;
        }
    </style>
</head>

<body>
    <h1>Welcome! {{num_of_boxes}}</h1>
    <div class="box-container">
        {% for x in range(0, num_of_boxes) %}
        <div class="box" style="background-color: {{color}};"></div>
        {% endfor %}
    </div>
</body>

</html>