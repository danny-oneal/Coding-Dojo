/*
    You can parse the text looking for patterns of perspectives to determine if it's first, second, or third.
    
    function getPerspective(text) {
        var firstPersonPerspectives = [...]
        var secondPersonPerspectives = [...]
        var thirdPersonPerspectives = [...]

        if(text contains firstPersonPerspectives) return 1;
        if(text contains secondPersonPerspectives) return 2;
        if(text contains thirdPersonPerspectives) return 3;
    }

    The hardest part of problem-solving is knowing if your solution is optimal.
*/